优先队列：

普通队列：先进先出
优先队列：出对顺序和入队顺序无关；和优先级有关

应用：任务管理器，动态选择优先级最高的任务执行


时间复杂度：
底层实现         入队      出队（拿出最大元素）
普通线性结构      O(1)     O(n)

顺序线性结构      O(n)     O(1)

堆              O(logn)   O(logn)

